name: Build and Deploy
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
    - uses: actions/checkout@v3
    - name: Install and Build 🔧
      run: |
        npm run install
        npm run build
    - name: Deploy 🚀
      uses: italktocomputers/italktocomputers.github.io/@v1
      with:
        folder: public/dist
